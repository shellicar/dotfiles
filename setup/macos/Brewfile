# Dependencies the dotfiles configs rely on.
# (Broader toolchain — azure-cli, docker, etc. — goes here too if you want it.)

brew "git"        # newer than the system git
brew "coreutils"  # provides gls (the `ls` alias)
brew "fnm"        # Node version manager
brew "pnpm"       # native install, not corepack
brew "tmux"       # .tmux.conf and the prompt status hooks
brew "vim"        # EDITOR, .vimrc, the vi alias
# GnuPG is built from source by build-gnupg.sh, not installed here: it carries a
# local scdaemon patch, and a package manager that always installs the latest
# version would pair a new gpg-agent with the vendored scdaemon. These are what
# that build links against.
brew "libgpg-error"
brew "libgcrypt"
brew "libassuan"
brew "libksba"
brew "npth"
brew "gnutls"
brew "pinentry-mac" # gpg-agent's pinentry-program
brew "ykman"        # gpg-setup.sh --hardware, and ykwhich
brew "autoconf"     # build-gnupg.sh runs autogen.sh
brew "automake"
brew "pkg-config"
brew "git-lfs"
brew "shellcheck" # ./test.sh
