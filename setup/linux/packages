# APT packages the dotfiles configs rely on.
# (Linux ls is already GNU, so no coreutils needed here.)

# curl is used by setup.sh itself (fnm, pnpm, GitVersion), not by a config.
curl
git
tmux
vim
gnupg
xclip
shellcheck
