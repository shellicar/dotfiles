diff --git a/scd/app-openpgp.c b/scd/app-openpgp.c
index 7390bc97a..4242afb47 100644
--- a/scd/app-openpgp.c
+++ b/scd/app-openpgp.c
@@ -5595,7 +5595,7 @@ do_sign (app_t app, ctrl_t ctrl, const char *keyidstr, int hashalgo,
     }
   rc = iso7816_compute_ds (app_get_slot (app), exmode, data, datalen, le_value,
                            outdata, outdatalen);
-  if (gpg_err_code (rc) == GPG_ERR_TIMEOUT)
+  if (!opt.keep_chv_on_timeout && gpg_err_code (rc) == GPG_ERR_TIMEOUT)
     clear_chv_status (app, ctrl, 1);
   else if (!rc && app->force_chv1)
     {
@@ -5769,7 +5769,7 @@ do_auth (app_t app, ctrl_t ctrl, const char *keyidstr,
       rc = iso7816_internal_authenticate (app_get_slot (app), exmode,
                                           indata, indatalen, le_value,
                                           outdata, outdatalen);
-      if (gpg_err_code (rc) == GPG_ERR_TIMEOUT)
+      if (!opt.keep_chv_on_timeout && gpg_err_code (rc) == GPG_ERR_TIMEOUT)
         clear_chv_status (app, ctrl, 1);
 
       /* Verify the result, when CHALLENGE_GENERATED */
@@ -6080,7 +6080,7 @@ do_decipher (app_t app, ctrl_t ctrl, const char *keyidstr,
           *outdatalen = *outdatalen + 1;
         }
     }
-  if (gpg_err_code (rc) == GPG_ERR_TIMEOUT)
+  if (!opt.keep_chv_on_timeout && gpg_err_code (rc) == GPG_ERR_TIMEOUT)
     clear_chv_status (app, ctrl, 1);
 
   if (gpg_err_code (rc) == GPG_ERR_CARD /* actual SW is 0x640a */
diff --git a/scd/scdaemon.c b/scd/scdaemon.c
index 48a7def74..ce95b9f21 100644
--- a/scd/scdaemon.c
+++ b/scd/scdaemon.c
@@ -96,6 +96,7 @@ enum cmd_and_opt_values
   octapiDriver,
   opcscDriver,
   opcscShared,
+  oKeepChvOnTimeout,
   oDisableCCID,
   oDisableOpenSC,
   oDisablePinpad,
@@ -155,6 +156,8 @@ static gpgrt_opt_t opts[] = {
   ARGPARSE_s_s (opcscDriver, "pcsc-driver",
                 N_("|NAME|use NAME as PC/SC driver")),
   ARGPARSE_s_n (opcscShared, "pcsc-shared", "@"),
+  ARGPARSE_s_n (oKeepChvOnTimeout, "keep-chv-on-timeout",
+                N_("keep the CHV status after a touch timeout")),
   ARGPARSE_s_n (oDisableCCID, "disable-ccid",
 #ifdef HAVE_LIBUSB
                                     N_("do not use the internal CCID driver")
@@ -614,6 +617,7 @@ main (int argc, char **argv )
         case octapiDriver: opt.ctapi_driver = pargs.r.ret_str; break;
         case opcscDriver: opt.pcsc_driver = pargs.r.ret_str; break;
         case opcscShared: opt.pcsc_shared = 1; break;
+        case oKeepChvOnTimeout: opt.keep_chv_on_timeout = 1; break;
         case oDisableCCID: opt.disable_ccid = 1; break;
         case oDisableOpenSC: break;
 
diff --git a/scd/scdaemon.h b/scd/scdaemon.h
index d6114c9e7..dc1c3dc5a 100644
--- a/scd/scdaemon.h
+++ b/scd/scdaemon.h
@@ -63,6 +63,7 @@ struct
   int allow_admin;     /* Allow the use of admin commands for certain
                           cards. */
   int pcsc_shared;     /* Use shared PC/SC access.  */
+  int keep_chv_on_timeout; /* Do not reset CHV status after a timeout.  */
   strlist_t disabled_applications;  /* Card applications we do not
                                        want to use. */
   unsigned long card_timeout; /* Disconnect after N seconds of inactivity.  */
